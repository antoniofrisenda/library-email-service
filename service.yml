apiVersion: v1
kind: Service
metadata:
  name: email-service
spec:
  selector:
    app: email-service
  ports:
    - name: web
      protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP